# =============================================================================
# Seattle LMS - Environment Variables (Railway Deployment)
# =============================================================================
# Copy relevant values to Railway environment variables (not .env file in git!)
# =============================================================================

# ============================
# DATABASE (REQUIRED - MongoDB Atlas)
# ============================
DATABASE_URL=mongodb+srv://seattle_user:PASSWORD@unifiedlms.lvywnng.mongodb.net/seattle-lms?retryWrites=true&w=majority&appName=UnifiedLMS

# ============================
# SECURITY (REQUIRED - Generate NEW values for production!)
# ============================
# Generate with: openssl rand -base64 32
JWT_SECRET=generate-new-secret-do-not-reuse-old-value

# ============================
# SERVER CONFIGURATION
# ============================
NODE_ENV=production
# Railway automatically sets PORT - no need to configure
# API_PORT will be set to ${{PORT}} in Railway
ENVIRONMENT=production

# ============================
# AWS S3 CONFIGURATION (Use existing Seattle credentials)
# ============================
AWS_ACCESS_KEY_ID=your-aws-access-key-id-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key-here
AWS_S3_BUCKET_NAME=seattle-lms-prod
AWS_REGION=us-east-1

# ============================
# EMAIL SERVICE - POSTMARK (Use existing Seattle token)
# ============================
POSTMARK_SERVER_TOKEN=your-postmark-server-token-here
POSTMARK_FROM_EMAIL=noreply@seattlelms.com

# ============================
# WEBFLOW API (Use existing Seattle token)
# ============================
WEBFLOW_TOKEN=your-webflow-api-token-here

# ============================
# CORS CONFIGURATION
# ============================
CORS_ALLOWED_ORIGINS=https://seattle-lms.webflow.io,https://studio.apollographql.com

# =============================================================================
# NOTES FOR RAILWAY DEPLOYMENT
# =============================================================================
# 1. DO NOT commit .env file to git
# 2. Add these variables in Railway dashboard under "Variables"
# 3. Generate NEW JWT_SECRET (don't reuse old ones)
# 4. Update DATABASE_URL to match Atlas connection string
# 5. Verify AWS S3 bucket exists and credentials work
# =============================================================================
